<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.githubyss.mobile.common.kit"
    tools:ignore="LockedOrientationActivity">

    <application
        android:name=".alone.application.ComkitApplication"
        android:allowBackup="false"
        android:icon="@mipmap/comui_ic_launcher"
        android:label="@string/comkit_appName"
        android:roundIcon="@mipmap/comui_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/comui_app_gray_theme">

        <activity
            android:name=".alone.homepage.HomepageActivity"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/comui_activity_noActionBar_theme"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
